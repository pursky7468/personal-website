import type { ReactNode } from 'react'

// Root layout — html/body structure lives in [locale]/layout.tsx
export default function RootLayout({ children }: { children: ReactNode }) {
  return children
}
